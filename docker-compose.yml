services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "3001:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - LOCALAI_URL=http://localai:8080
      - LOCALAI_WHISPER_MODEL=whisper-1
      - LOCALAI_LLM_MODEL=gpt-4o-mini
      # Deepgram API key (optional - for deepgram provider)
      - DEEPGRAM_API_KEY=${DEEPGRAM_API_KEY:-}
    volumes:
      # Persist SQLite database and uploads
      - backend-data:/app/data
      - backend-uploads:/app/uploads
    depends_on:
      localai:
        condition: service_healthy
    restart: unless-stopped
    # Resource limits for backend
    deploy:
      resources:
        limits:
          memory: 512M

  localai:
    # AIO image includes whisper backend (required for transcription)
    image: localai/localai:latest-aio-gpu-nvidia-cuda-12
    ports:
      - "8080:8080"
    environment:
      - DEBUG=false
      - MODELS_PATH=/models
      - PARALLEL_REQUESTS=1
      # Pre-install whisper backend for transcription
      - LOCALAI_EXTERNAL_BACKENDS=whisper:quay.io/go-skynet/local-ai-backends:latest-gpu-nvidia-cuda-12-whisper
    volumes:
      # Mount local models folder
      - ./models:/models
      # Persist build cache across restarts
      - localai-cache:/build
      # Persist downloaded backend binaries (whisper, llama-cpp, etc.)
      - localai-backend-data:/tmp/localai/backend_data
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/readyz"]
      interval: 30s
      timeout: 10s
      retries: 10
      start_period: 180s  # Models take time to load on first run
    restart: unless-stopped

volumes:
  backend-data:
  backend-uploads:
  localai-cache:
  localai-backend-data:
